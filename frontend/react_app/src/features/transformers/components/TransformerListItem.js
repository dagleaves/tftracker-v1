import React from 'react';
import Box from '@mui/material/Box';

export const TransformerListItem = (transformer) => {

    return (
        <React.Fragment>
            <Box>
                
            </Box>
        </React.Fragment>
    )
}